{
  "$schema": "http://json-schema.org/schema",
  "id": "ng-add",
  "title": "Nebular Theme ng-add schematics",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "theme": {
      "type": "string",
      "description": "The theme will be installed.",
      "default": "default"
    }
  }
}
