{
  "$schema": "http://json-schema.org/schema",
  "id": "component",
  "title": "New Nebular Playground Example Component Schema",
  "type": "object",
  "properties": {
    "component": {
      "type": "string",
      "description": "The name of the component the example is for.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "name": {
      "type": "string",
      "description": "The name of the example component.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "selector": {
      "type": "string",
      "format": "html-selector",
      "description": "The selector to use for the component."
    },
    "prefix": {
      "type": "string",
      "format": "html-selector",
      "description": "The prefix to apply to generated selectors.",
      "alias": "p"
    },
    "styleext": {
      "description": "The file extension to be used for style files.",
      "type": "string",
      "default": "scss"
    }
  },
  "required": [
    "component",
    "name"
  ]
}
